<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="utf-8" />
        <title>Inversor</title>
        <link href="/styles/styles.css" rel="stylesheet" />
        <link href="https://cdn.jsdelivr.net/npm/simple-datatables@7.1.2/dist/style.min.css" rel="stylesheet" />
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
        <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
        <link rel="stylesheet" href="https://unpkg.com/leaflet@1.7.1/dist/leaflet.css" />

    </head>
    <body class="sb-nav-fixed">
        <h1>Selección de bateria</h1>
        <main>
            <div class="row">
                <div class="col-xl-8">
                    <div class="card mb-4">
                        <div class="card-header">
                            <i class="fas fa-chart-area me-1"></i>
                            Selección de equipo
                        </div>
                        <div class="card-body"><canvas id="myAreaChart" width="100%" height="40"></canvas>
                                <form class="form-floating" action="/bateria" method="post">
                                    <div id="menu" align="left"> 
                                        <br>
                                        <label for="nombre">Modelo:</label>
                                        <select id="nombre" name="nombre">
                                            <option></option>
                                            <% modBat.forEach(item => { %>
                                                <option value="<%= item.modelo %>">
                                                    <%= item.modelo %>
                                                </option>
                                            <% }); %>
                                        </select>
                                        <br>
                                        <label for="tension">Tensión nominal:</label>
                                        <input type="text" id="tension" name="tension" readonly> Vdc
                                        <br>
                                        <label for="cap">Capacidad:</label>
                                        <input type="text" id="cap" name="cap" readonly> Wh
                                        <br>
                                        <p></p>
                                        <input type="submit" name="guardarCoordenadas" id="guardarCoordenadas" value="Guardar" style="font-size:20px">
                                        
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
        
        <!-- JS de Bootstrap (incluyendo los componentes de Bootstrap) -->
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
        <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.8.0/Chart.min.js" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/simple-datatables@7.1.2/dist/umd/simple-datatables.min.js" crossorigin="anonymous"></script>
 

    <!-- Tu código JavaScript personalizado aquí -->
    <script>
        var modBat = <%- JSON.stringify(modBat) %>; // Convertir a JSON para asegurarse de que se interprete correctamente
        document.addEventListener("DOMContentLoaded", function() {
            const selectModelo = document.getElementById("nombre");
            const tension = document.getElementById("tension");
            const cap = document.getElementById("cap");
            // Agrega más variables para los otros campos
            
            selectModelo.addEventListener("change", function() {
                const selectedModelo = selectModelo.value;
                const selectedModeloObj = modBat.find(item => item.modelo === selectedModelo);
                //modelo, tension, cap
                if (selectedModeloObj) {
                    tension.value = selectedModeloObj.tension;
                    cap.value = selectedModeloObj.cap;
                    // Actualiza los otros campos input con la misma lógica
                } else {
                    tension.value = "";
                    cap.value = "";
                
                    // Limpia los otros campos
                }
            });
        });    
    </script>
    </body>
</html>